INSERT_UPDATE SearchRestriction; code[unique = true]      ; query                                                                                              ; principal(UID); restrictedType(code); active; generate
                               ; OnlineCatalogRestriction ; {catalogVersion} IN ({{SELECT {cv.PK} FROM {CatalogVersion as cv} WHERE {cv.version} = 'Online'}}) ; anonymous     ; News                ; true  ; false
